// ============================================================
// 수비 엔진 물리 설정
// 리그별 환경 차이를 이 파일 한 곳에서 관리
// ============================================================

export const PHYSICS_CONFIG = {
  /**
   * Magnus carry_factor 최대값
   * - KBO 기준: 0.22  (EV 170 km/h 타구 기준 ~+22% 비거리)
   * - MLB 기준: 0.26  (저마찰 공 + 높은 EV, 2019 이후 Statcast 기반)
   * contact_quality=1.0일 때 carry_factor = 1 + carry_factor_max
   */
  carry_factor_max: 0.26,

  /**
   * 외야수 스프린트 속도 범위 (m/s)
   * defence 스탯 0~100 → min~max 선형 보간
   * - KBO 평균 외야수: 6.0 m/s 수준
   * - MLB 평균 외야수: 6.7 m/s, 상위권: 7.5 m/s
   */
  outfielder_speed_min: 5.0,
  outfielder_speed_max: 7.0,

  // ── 4구간 포구 모델 ──────────────────────────────────────
  /** 여유 도달 경계 (초) — 이 이상이면 루틴 플레이 */
  CATCH_CLEAN_THRESHOLD: 0.3,
  /** 반응/다이빙 캐치 한계 (초) — 이 이상 지나가면 포구 불가 */
  CATCH_SNAG_LIMIT: 0.20,

  // ── 펜스 높이 ──────────────────────────────────────────
  /** 외야 펜스 높이 (m) — MLB 평균 약 2.4m (8ft). 구장별 오버라이드 예정 */
  FENCE_HEIGHT: 2.4,

  // ── 파울 영역 설정 ──────────────────────────────────────
  /** 페어 영역 경계 (°) — |theta| ≤ 이 값이면 페어 */
  FAIR_ANGLE: 45,
  /** 수비 가능 파울 영역 끝 (°) — 이 이상이면 관중석 (구장별 오버라이드 예정) */
  STANDS_ANGLE: 60,
  /** 방향각 최대 범위 (°) — 180° = 완전 뒤쪽 (백네트) */
  MAX_DIRECTION_ANGLE: 180,

  // ── 에러 시 줍는 시간 ────────────────────────────────────
  /** 에러 발생 후 공 줍는 기본 시간 (초) */
  error_pickup_base: 1.5,
  /** 수비력에 의한 줍는 시간 감소폭 (초) — defence 100일 때 base - reduction */
  error_pickup_reduction: 0.5,

  // ── 파울 팁 ──────────────────────────────────────────────
  /** 2스트라이크 파울 중 파울팁→삼진 확률 */
  foul_tip_prob: 0.05,
}
